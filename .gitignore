# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Surfer / Firefox build outputs
engine/
build/
dist/
.surfer/*
!.surfer/dynamicConfig.brand.json
obj-*/

# Logs
*.log

# OS / IDE noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# TypeScript incremental build cache
*.tsbuildinfo

# Dev profile (persisted across `npm run dev` launches)
.bento-dev-profile/

# Local-only dev notes (per-developer scratch / workflow docs)
.dev-notes/

# Local MCP overrides / scratch config
.mcp.local.json
.mcp.*.local.json
.codex-mcp/

# Release artifacts (built by scripts/build-release.sh / .github/workflows/release.yml)
release-out/

# Plans
# plans/

# Bugs
# bugs/

# TS-compiled outputs of shared sources (we only want sources tracked)
extensions/_shared/*.js

# Auto-generated chrome design-token stylesheet — regenerated from Tale UI
# source on every `pnpm run import`. See docs/chrome-tokens.md.
src/browser/base/content/bento-chrome-tokens.css
