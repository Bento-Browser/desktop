# Bento Browser — Windows mozconfig.
. "$topsrcdir/../configs/common/mozconfig"

# Firefox's supported cross-language ThinLTO avoids fat-LTO peak memory in the final gkrust link.
export MOZ_LTO=cross

# TODO: Windows-specific flags (MSVC paths, installer signing, MOZ_AUTOMATION_PACKAGE_TESTS).
