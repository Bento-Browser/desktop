// First Ladle story — proves the lab boots and Tale UI renders correctly
// outside the shell entry point. M1 will add stories per layer-2 component
// (TabRow, PanelHandle, WorkspaceCard, ...).
import { App } from './App';

export const Default = () => <App />;
