{
  "name": "bento-browser",
  "version": "0.0.0",
  "private": true,
  "description": "Bento Browser — a Firefox-based browser built with @bento-browser/surfer (fork of @zen-browser/surfer).",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "download": "bash scripts/surfer-env.sh download",
    "bootstrap": "bash scripts/surfer-env.sh bootstrap",
    "import": "bash scripts/import.sh",
    "build": "pnpm run ext:build && pnpm run import && bash scripts/surfer-env.sh build && bash scripts/sync-builtin-addon-symlinks.sh",
    "build:ui": "bash scripts/surfer-env.sh build --ui && bash scripts/sync-builtin-addon-symlinks.sh",
    "package": "bash scripts/surfer-env.sh package",
    "lc": "bash scripts/surfer-env.sh license-check",
    "mach": "bash scripts/mach-raw.sh",
    "mach:build": "bash scripts/mach-raw.sh build",
    "build:full": "bash scripts/build-bento.sh",
    "build:release": "bash scripts/build-release.sh",
    "brand:regen": "bash scripts/regen-branding.sh",
    "dev": "pnpm run ext:build && pnpm run import && bash scripts/dev-profile-clean.sh && bash scripts/dev-launch.sh",
    "dev:fresh": "pnpm run ext:build && pnpm run import && bash scripts/dev-launch.sh $(mktemp -d)",
    "shell:dev": "pnpm --filter @bento/shell dev",
    "shell:build": "pnpm --filter @bento/shell build",
    "shell:sync": "pnpm run shell:build && bash scripts/sync-shell.sh",
    "shell:ladle": "pnpm --filter @bento/shell ladle:serve",
    "tools:build": "pnpm --filter @bento/tools build",
    "ext:build": "pnpm --filter @bento/shell build && pnpm --filter @bento/tools build",
    "size": "pnpm run ext:build && size-limit",
    "size:check": "size-limit"
  },
  "devDependencies": {
    "@bento-browser/surfer": "github:Bento-Browser/surfer#358ca0418217c59fb659acd05ce7996682105147",
    "@eslint/js": "^9.0.0",
    "@size-limit/file": "^12.1.0",
    "eslint": "^9.0.0",
    "husky": "^9.1.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.3.0",
    "size-limit": "^12.1.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.0.0"
  },
  "license": "MPL-2.0"
}
